# =============================================================
# PostgreSQL ClusterIP Service
#
# Only applications inside Kubernetes
# should access PostgreSQL.
#
# =============================================================

apiVersion: v1
kind: Service

metadata:
  name: postgres-service
  namespace: devsecops

spec:

  selector:

    app: postgres

  ports:

  - port: 5432

    targetPort: 5432

  type: ClusterIP
